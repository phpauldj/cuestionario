<? if (is_array($this->flashMessages)) : ?>
    <? foreach ($this->flashMessages as $msg) : ?>
        <div class="flash-message <?= $msg->level ?>">
            <div class="msgsTime msgYTIE">
                <div class="contenidoFlashMessage <?=
        ($msg->level == "success" ? "msgYellow" : ($msg->level == "info" ? "msgBlue" : (
                $msg->level == "error" ? "msgRed" : "")))
        ?> mB10 r5">
                    <?= $this->escape($msg->message) ?>
                </div>
            </div>
        </div>
    <? endforeach; ?>
<? endif; ?>